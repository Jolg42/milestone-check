name: 'PR Milestone Check'
description: 'An action for checking if a milestone is assigned to a PR'
author: 'Jolg42'
branding:
  icon: git-branch
  color: blue
inputs:
  github-token:
    description: 'Token for the repository. Can be passed in using {{ secrets.GITHUB_TOKEN }}'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
